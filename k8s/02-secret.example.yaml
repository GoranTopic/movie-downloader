# Template only - do not put real values here / do not commit real secrets.
# The real Secret is created directly with kubectl (see README-k8s.md), sourced
# from backend/.env, and is never written to a file in this repo.
apiVersion: v1
kind: Secret
metadata:
  name: movie-downloader-secrets
  namespace: movie-downloader
type: Opaque
stringData:
  TOKEN: "changeme"
  AUTH_SECRET: "changeme"
  SMTP_USER: "changeme"
  SMTP_PASS: "changeme"
